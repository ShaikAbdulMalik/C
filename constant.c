#include<stdio.h>
int main(){
    //constant is the fixed value that cannot be altered by the program during its execution
    //it uses the keyword 'const' to create a keyword that cannot be changed so easily 
    
    const float PI = 3.14159;
    //PI = 3.14154;
    printf("%f",PI);

    return 0;
}